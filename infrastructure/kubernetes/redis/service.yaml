apiVersion: v1
kind: Service
metadata:
  name: fn-redis
  namespace: fn
  labels:
    app: fn-redis
    component: cache
    app.kubernetes.io/name: fn-redis
    app.kubernetes.io/component: cache
    app.kubernetes.io/part-of: fn
spec:
  type: ClusterIP
  selector:
    app: fn-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
    - name: metrics
      port: 9121
      targetPort: metrics
      protocol: TCP
  sessionAffinity: None
