apiVersion: v1
kind: Service
metadata:
  name: fn-backend
  namespace: fn
  labels:
    app: fn-backend
    component: backend
    app.kubernetes.io/name: fn-backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: fn
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  selector:
    app: fn-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  sessionAffinity: None
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fn-backend-sa
  namespace: fn
  labels:
    app: fn-backend
    app.kubernetes.io/name: fn-backend
  annotations:
    # IRSA: bind to IAM role for AWS service access (S3, Secrets Manager, etc.)
    eks.amazonaws.com/role-arn: "arn:aws:iam::${AWS_ACCOUNT_ID}:role/fn-backend-role"
automountServiceAccountToken: false
